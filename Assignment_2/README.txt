Note: Make sure that the app has permission to storage, both read and write.
Part 1: change the build tool and every thing to api 23
Part 2: Use a counter to determing where the next picture should go. Used a GridView with an adapter and an
    array list to display images
Part 3: Used getExternalStoragePublicDirectory to get the public directory then created a sub folder where
    all the new pictures will be saved and overriden;